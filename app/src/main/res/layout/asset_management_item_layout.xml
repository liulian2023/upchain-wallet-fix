<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="15dp"
    android:paddingBottom="15dp"
    >

    <ImageView
        android:id="@+id/delete_iv"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginStart="15dp"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="15dp"
        android:src="@drawable/add_coin_icon"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/asset_iv"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginStart="10dp"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="15dp"
        android:src="@drawable/wallet_logo_demo"
        app:layout_constraintBottom_toBottomOf="@+id/delete_iv"
        app:layout_constraintStart_toEndOf="@+id/delete_iv"
        app:layout_constraintTop_toTopOf="@+id/delete_iv" />

    <TextView
        android:id="@+id/asset_name_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="18dp"
        android:text="name"
        app:layout_constraintStart_toEndOf="@+id/asset_iv"
        app:layout_constraintTop_toTopOf="@+id/asset_iv" />

    <TextView
        android:id="@+id/asset_address_tv"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginRight="100dp"
        android:singleLine="true"
        android:text="address"
        app:layout_constraintBottom_toBottomOf="@+id/asset_iv"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/asset_name_tv"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/asset_amount_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="amount"
        app:layout_constraintBottom_toBottomOf="@+id/asset_iv"
        app:layout_constraintStart_toStartOf="@+id/asset_name_tv" />
</androidx.constraintlayout.widget.ConstraintLayout>